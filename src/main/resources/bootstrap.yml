spring:
  application.name: service-discovery-server
  profiles:
    active: default
#  removing cloud config prop
#  cloud:
#    config:
#      uri: http://localhost:8899

host.environment.port: 8761
